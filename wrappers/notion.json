{
  "name": "notion-mcp",
  "description": "Notion integration via official MCP server",
  "upstream": {
    "package": "@notionhq/notion-mcp-server",
    "version": "^2.2.1"
  },
  "auth": {
    "type": "apiKey",
    "envVar": "NOTION_TOKEN",
    "notes": "Create an integration at https://www.notion.so/my-integrations and grant it access to the target pages/databases. v2 uses a raw bearer token in NOTION_TOKEN (v1 OPENAPI_MCP_HEADERS env is no longer supported)."
  },
  "desktop_config": {
    "command": "npx",
    "args": ["-y", "@notionhq/notion-mcp-server"],
    "env": {
      "NOTION_TOKEN": "${NOTION_TOKEN}"
    }
  }
}
